# config/services.yaml
parameters:
    stripe.secret_key: "%env(STRIPE_SECRET_KEY)%"
    app.admin_email: "%env(APP_ADMIN_EMAIL)%"
services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: "../src/"
